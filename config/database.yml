default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS", 5) %>

production:
  <<: *default
  url: "<%= ENV['DATABASE_URL'] %>"

development:
  adapter: sqlite3
  database: db/development.sqlite3
  pool: 5
  timeout: 5000